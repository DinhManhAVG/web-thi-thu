services:
  web-thi-thu:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: web-thi-thu:latest
    container_name: web-thi-thu
    ports:
      - "5173:80"
    restart: unless-stopped
