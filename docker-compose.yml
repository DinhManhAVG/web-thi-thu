services:
  web-thi-thu:
    build:
      context: ./app
      dockerfile: Dockerfile
    image: web-thi-thu:latest
    container_name: web-thi-thu
    ports:
      - "3497:80"
    restart: unless-stopped
